# FishNote — 리디자인 구현 설계 (Codex용)

> 시안: `docs/design-reference-v2.html` — **`.screen` 스코프 안의 `--p-*` 토큰과 마크업이 제품 기준**. 문서 래퍼(제안서 껍데기) 스타일은 무시할 것.
> 원칙: **기능 로직(API 연동·라우팅·상태관리)은 그대로, 스타일·마크업·카피만 교체.** 예외는 신규 기능 2건(U1 사진 업로드, R5 캘린더 월 셀렉터 교체)뿐.
> 서비스명은 **FishNote로 확정** — 화면에 보이는 모든 브랜드 표기를 통일한다.
> 순서: R0(토큰) → R1(공통 컴포넌트) → R2(레이아웃) → R3(홈) → R4(상세) → R5(캘린더·저장) → R6(후기) → U1·U2(사진 업로드) → R7(상태·카피) → R8(검수).

---

## 진행 체크리스트
- [x] R0 디자인 토큰 + 폰트
- [x] R1 공통 컴포넌트 (뱃지·칩·별점·가격·시즌바·FishCard)
- [x] R2 레이아웃·헤더 + FishNote 표기 통일
- [x] R3 홈 (히어로 → 이달의 제철 → 규칙형 추천 → 전체 도감)
- [x] R4 상세 (갤러리·한눈 스펙·팁·비슷한 횟감)
- [x] R5 제철 캘린더 + 저장한 도감
- [x] R6 후기 UI (분포·정렬·도움돼요)
- [x] U1 BE 이미지 업로드 API
- [x] U2 FE 후기 사진 첨부
- [x] R7 로딩·빈 상태·에러·마이크로카피
- [x] R8 자동 검수 구현(Vitest·Playwright·axe)
- [ ] R8 운영/실기기 시각 QA(원격 이미지 200·crop, 200% zoom, 실제 Vercel/Cloudinary)

---

## 0. 디자인 3원칙 (모든 티켓의 판단 기준)

1. **"지금 제철"만 색으로 강조** — 포인트 컬러(물빛)는 지금 제철 뱃지·주요 버튼·링크에만. 나머지는 중성색.
2. **전문용어 제로** — 월 배열은 "제철 6–8월", priceLevel은 "₩₩ 보통"처럼 항상 사람의 말로 번역해서 표시.
3. **한 화면, 한 가지 일** — 홈=찾기, 상세=알기, 캘린더=고르기. 장식성 요소(배너·팝업) 추가 금지.

---

## R0 — 디자인 토큰 + 폰트

**프롬프트**:
> `FE/tailwind.config.js`의 theme.extend를 아래 토큰으로 교체하고, `npm i pretendard` 후 `main.tsx`에서 `import 'pretendard/dist/web/variable/pretendardvariable-dynamic-subset.css'`를 추가해줘. `index.css`의 body 기본값: `bg-mist text-ink`, font-size 15px, line-height 1.7. 기존 클래스 사용처가 깨지지 않게 빌드 확인.

```js
// tailwind.config.js — theme.extend
colors: {
  sea:  { DEFAULT: '#0F6E84', soft: '#E3F0F3' }, // 물빛(유일한 포인트)
  ink:  { DEFAULT: '#1A2B33', mute: '#5F7078' }, // 텍스트 / 보조 텍스트
  mist: '#F6F8F9',                               // 페이지 배경
  line: '#E3EAED',                               // 경계선
  chipbg: '#EEF3F5',                             // 칩 배경
  star: '#E0A030',                               // 별점 전용(포인트 아님)
},
borderRadius: { card: '14px' },
fontFamily: { sans: ['Pretendard Variable','Pretendard','Apple SD Gothic Neo','Noto Sans KR','sans-serif'] },
```

**타이포 스케일**: 디스플레이 28/800 · 섹션 타이틀 19/800 · 본문 15/400 · 캡션 13 · 라벨(뱃지·칩) 12/700. 숫자가 나오는 곳은 `tabular-nums`.

**DoD**: 토큰 적용·Pretendard 렌더·빌드 통과.

---

## R1 — 공통 컴포넌트 스펙

**프롬프트**:
> 시안 `.screen` 스코프 기준으로 아래 스펙대로 공통 컴포넌트를 스타일링/작성해줘. props와 데이터 필드는 기존 그대로 사용.

### 제철 뱃지 (상태 2개뿐)
- **지금 제철**: `seasonMonths.includes(현재 월)`이면 표시. bg `sea` / 흰 글자 12/700 / rounded-full / px 10 py 3 / 앞에 5px 흰 점. **물빛을 채워 쓰는 유일한 뱃지.**
- **제철 기간**: 흰 배경 + `line` 테두리 + `ink-mute` 12/600. 라벨 규칙(`lib/format.ts`에 유틸):
  - 연속 구간 → `제철 6–8월`, 12개월 전부 → `연중`, 겨울처럼 해를 넘는 경우 12→1 연결 판정 → `제철 11–2월`.

### 가격대
- `'₩'.repeat(priceLevel)` + 라벨 `{1: '부담 없는', 2: '보통', 3: '특별한 날'}`.
- 카드에서는 기호만(13/700), 상세·필터에서는 `₩₩ 보통`처럼 기호+라벨(라벨은 `ink-mute`).

### 별점
- 표기: `★ 4.3 (12)` — star 색 ★ 1개 + 평균 소수 1자리 + 회색 괄호 후기 수. 아이콘 단독 사용 금지.
- 후기 0개: 카드에서는 별점 영역 생략, 상세에서는 "아직 후기가 없어요".

### 맛 태그 / 필터 칩
- 기본: bg `chipbg` / `ink` 13/600 / rounded-full / px 13 py 5.
- 선택(필터): bg `sea` / 흰 글자 + `✕`로 해제. 드롭다운 트리거 칩에는 `▾`.

### SeasonBar (상세 전용)
- 12칸 그리드, 칸 높이 8px rounded gap 3px. 제철 월 = `sea` 채움, 나머지 `chipbg`.
- 현재 월 칸 위에 "지금" 10px/700 `sea` 마커, 칸 아래 월 숫자 10px `ink-mute`.

### FishCard
- 흰 배경 / `line` 1px 테두리 / rounded-card / 그림자 없음(호버 시만 살짝).
- 이미지 4:3 고정(`aspect-ratio` + `object-cover`), 좌상단(10px) 지금 제철 뱃지, 우상단(8px) 저장 하트 — 32px 원형, bg `white/90`, 저장 시 `sea` 채운 하트.
- 본문(p-3.5): 이름 16/700 + 별점 우측 정렬 → 한 줄 소개 13 `ink-mute`(1줄 ellipsis) → 메타 행(제철 기간 뱃지 좌 / 가격 우).

**DoD**: 스토리 화면 또는 홈에서 각 상태(지금 제철·저장됨·선택 칩) 확인, 실데이터 렌더 정상.

---

## R2 — 레이아웃·헤더 + FishNote 통일

**프롬프트**:
> `AppLayout.tsx`를 시안대로: 흰 배경 헤더 + 하단 `line` 1px. 좌측 로고(물고기 아이콘 + "FishNote" 17/800), 메뉴 3개 "도감 · 제철 캘린더 · 저장한 도감"(14/600 `ink-mute`, 활성 `sea`). **홈(`/`)에서는 헤더 검색을 숨기고**, 서브 페이지에서만 우측에 컴팩트 검색(rounded-full, `mist` 배경) 표시. 본문 컨테이너 max-w 980px. 그리고 화면에 보이는 브랜드 표기(로고·`index.html` title·푸터 등)를 전부 "FishNote"로 통일해줘.

**DoD**: 3메뉴 라우팅 동작, 홈에서 헤더 검색 미노출, 브랜드 표기 통일.

---

## R3 — 홈 (HomePage)

구성 순서(위→아래), 데이터는 전부 기존 API:

| 섹션 | 내용 | 데이터 |
|---|---|---|
| ① 검색 히어로 | 중앙 정렬. "오늘 이 회, 무슨 생선일까요?" 28/800 + 서브 "이름·제철·맛·가격대까지, 3초면 확인해요" + 큰 검색창(max-w 520, 우측 `sea` 검색 버튼) + "많이 찾는 생선" 정적 태그 4개(광어·방어·연어·참돔, 클릭 시 검색) | 정적 |
| ② 이달의 제철 | 제목 "N월, 지금이 제철이에요"(N=현재 월 자동) + 우측 "제철 캘린더 →" 링크. 카드 4개 그리드(모바일 가로 스와이프) | `GET /fish?month=N` |
| ③ 에디터 추천 | 낮은 가로형 카드 2장(이미지 aspect 5:2), 소개 문구를 추천 이유처럼 노출 | `GET /fish?featured=true` 앞 2개 |
| ④ 전체 도감 | 제목 + 종 수. 필터 행: 칩 3개(계절·맛·가격대 드롭다운) + 우측 정렬 세그먼트(인기순/이름순). 카드 4열 그리드(태블릿 2열, 모바일 1열) | `GET /fish` + 기존 필터 상태 |

**프롬프트**:
> 위 표와 시안 기준으로 `HomePage.tsx`, `SearchBar.tsx`, `FilterChips.tsx`를 스타일링해줘. useFishList 훅·필터 상태 로직은 그대로 유지하고 마크업/클래스만 교체. 히어로 검색은 제출 시 기존 `/search` 흐름 그대로. 선택된 필터 칩은 `sea` 채움 + ✕ 해제, "필터 초기화" 텍스트 버튼 포함.

**DoD**: 4개 섹션 렌더, 이달의 제철이 현재 월 자동 반영, 필터·검색·카드 클릭 정상.

---

## R4 — 상세 (FishDetailPage)

상단 2컬럼 그리드(1.05fr : 1fr, 모바일 1열), 이하 순서 고정:

1. **좌: 갤러리** — 메인 4:3 rounded-16 + 아래 썸네일 4칸(선택 썸네일 `sea` 2px outline). 데이터 `images` 배열, 없으면 대표 1장만.
2. **우: 핵심 정보** — "← 도감으로" → 지금 제철 뱃지 → 이름 26/800 + 영문명 14 `ink-mute` → 한 줄 소개 → `★ 4.8 · 후기 6개` → **한눈 스펙 3칸**(테두리 카드, 칸: 제철 `6–8월` / 맛 `부드러운 · 담백` / 가격대 `₩₩₩ 특별한 날`) → SeasonBar → 버튼 행: `♡ 저장하기`(primary) + `후기 쓰기`(ghost, 후기 폼으로 스크롤).
3. **어떤 맛인가요?** — `tasteDesc` 본문(max-w 640).
4. **이렇게 즐겨요** — `tips` 배열을 카드형 리스트로(순서 보존).
5. **비슷한 생선** — 3열 카드(FishCard 재사용, 소개 생략 가능).
6. **후기** — R6 스펙.

**프롬프트**:
> 위 구조와 시안 기준으로 `FishDetailPage.tsx`, `SeasonBar.tsx`를 스타일링해줘. useFishDetail·useReviews 연동은 그대로. 스크롤 없이 첫 화면에서 1·2번(사진·이름·지금 제철·스펙 3칸·시즌바)이 보이는 게 목표.

**DoD**: 상세 전 섹션 시안과 일치, 비슷한 생선 클릭 이동, 저장 토글 동작.

---

## R5 — 제철 캘린더 + 저장한 도감

**프롬프트 (캘린더)**:
> `CalendarPage.tsx`를 개편해줘. 기존 `MonthSidebar`(세로) 대신 **가로 12칸 월 셀렉터**(데스크톱 한 줄 grid-cols-12, 모바일 가로 스크롤 칩). 각 칸: 월 14/700 + 아래 종 수 11px. 선택 월 = `sea` 채움 + 흰 글자, 현재 월엔 "지금" 표기. **기본 선택은 오늘이 속한 달.** 선택 월 목록은 기존 `GET /fish?month=N` 유지. 월별 종 수는 `GET /fish` 전체 1회 조회 후 각 생선의 `seasonMonths`로 클라이언트 계산(생선 수가 적어 충분). 아래는 FishCard 그리드.

**프롬프트 (저장한 도감)**:
> `SavedPage.tsx`는 기존 localStorage 로직 유지, 새 FishCard와 빈 상태(R7 카피)만 적용해줘.

**DoD**: 진입 시 이번 달 자동 선택, 월 전환·종 수 표시 정상, 저장 목록 새 카드로 렌더.

---

## R6 — 후기 UI (ReviewList / ReviewForm)

레이아웃: 좌 250px 요약 패널 + 우 목록(모바일 1열, 요약이 위).

- **요약 패널**: 평균 `4.8 / 5` 34/800 → 별 행 → "후기 6개" → 별점 분포 5→1 (라벨 `5점` / 막대 track `chipbg`·fill `sea` h-6px / 개수). 데이터 `ratingDistribution`.
- **정렬**: 칩 2개 `최신순`(기본) / `도움순` → `sort=latest|helpful`.
- **후기 카드**: 아바타(32px 원형 `sea-soft` 배경 + 닉네임 첫 글자 `sea`) + 닉네임 14/700 + 별점 + 날짜 12 `ink-mute`, 우측 끝 "삭제" 12 `ink-mute`(클릭 → 비밀번호 입력 모달 → DELETE). 본문 14. 사진 있으면 썸네일(U2). 하단 **도움돼요 필**: 기본 `👍 도움돼요 4` / 누른 후 `👍 도움돼요 5 · 눌렀어요`(`sea` 테두리+`sea-soft` 배경, 비활성). 중복 방지 localStorage `helpful:<reviewId>`.
- **작성 폼**(목록 하단, 모달 아님 — 항상 펼쳐진 카드): 닉네임 / **별점 (선택)** 별 5개 탭 / 후기 textarea(placeholder "맛·식감·먹은 곳 분위기, 자유롭게 적어주세요") / 비밀번호 + 헬퍼 "후기를 지울 때만 써요 (4자 이상)" / `등록하기` primary. **이미지 URL 텍스트 입력은 제거**(U2의 파일 첨부로 대체). 검증 에러는 해당 필드 아래 13px로.

**프롬프트**:
> 위 스펙과 시안대로 `ReviewList.tsx`, `ReviewForm.tsx`를 구현/스타일링해줘. 작성·삭제 mutation과 invalidate 로직은 기존 유지. 도움돼요는 `POST /reviews/{id}/helpful` 연동 + localStorage 1회 제한.

**DoD**: 분포·정렬·도움돼요(1회 제한) 동작, 폼 검증 메시지 표시, URL 입력란 제거됨.

---

## U1 — BE 이미지 업로드 API (신규)

> 배경: 후기 사진은 사용자가 폰으로 찍어 올린다. **사용자는 URL을 보지도 입력하지도 않는다** — 파일을 고르면 서버가 이미지 호스팅(Cloudinary)에 올리고 URL을 만들어 기존 `imageUrl` 필드에 저장하는 구조.
> Railway 컨테이너는 재배포 시 디스크가 초기화되므로 서버 디스크 저장은 금지. 외부 스토리지 필수.
> 사전 준비(사람이 할 일): Cloudinary 무료 계정 생성 → `CLOUDINARY_URL` 환경변수 확보(형식 `cloudinary://<api_key>:<api_secret>@<cloud_name>`).

**프롬프트**:
> Spring Boot에 이미지 업로드 API를 추가해줘. `POST /api/v1/images` (multipart/form-data, 필드명 `file`). 의존성 `com.cloudinary:cloudinary-http5` 추가, 환경변수 `CLOUDINARY_URL`로 초기화하는 `config/CloudinaryConfig` 작성. 검증: 파일 필수, 최대 5MB, content-type `image/*`만 허용(위반 시 표준 400 에러). 업로드는 Cloudinary 폴더 `fishnote/reviews`, 응답은 `{ "url": "https://res.cloudinary.com/..." }` 201. `application.yml`에 `spring.servlet.multipart.max-file-size: 5MB` 설정. 전역 예외처리(§04명세 §7 포맷) 적용.

**DoD**: curl 파일 업로드 → Cloudinary URL 반환, 5MB 초과·비이미지 파일 400, 키는 환경변수로만 주입.

---

## U2 — FE 후기 사진 첨부 (신규)

**프롬프트**:
> `ReviewForm.tsx`에 사진 첨부를 추가해줘. "📷 사진 추가" 버튼 → `<input type="file" accept="image/*">`(1장). 선택 시 로컬 미리보기 썸네일 + 우상단 ✕ 제거 버튼. 등록 시 파일이 있으면 먼저 `POST /images`로 업로드하고 받은 url을 기존 `imageUrl` 필드에 넣어 `POST /fish/{id}/reviews` 호출(업로드 중 등록 버튼 스피너+비활성). 실패 시 "사진 업로드에 실패했어요. 사진 없이 등록하거나 다시 시도해 주세요" 표시. `ReviewList.tsx`에서 `imageUrl` 있으면 본문 아래 썸네일(rounded-10, max-h 160, object-cover, 클릭 시 원본 새 탭).

**DoD**: 폰 사진 첨부 → 후기 카드에 사진 표시, 미첨부 후기도 정상 등록.

---

## R7 — 상태·마이크로카피 (복붙용)

**프롬프트**:
> 아래 표의 문구와 상태 UI를 전 화면에 적용해줘. 로딩은 스피너 대신 **스켈레톤 카드**(4:3 블록 + 텍스트 라인 2개, `animate-pulse`, 목록은 카드 수만큼).

| 상황 | 문구 / UI |
|---|---|
| 검색 결과 없음 | 🐟 "'{검색어}'는 아직 도감에 없어요" + [전체 도감 둘러보기] |
| 필터 결과 없음 | "이 조건에 맞는 생선이 아직 없어요" + "필터를 하나 줄여보시면 어떨까요?" + [필터 초기화] |
| 저장한 도감 빈 상태 | "아직 저장한 생선이 없어요. 마음에 드는 카드의 ♡를 눌러 모아보세요" + [도감 둘러보기] |
| 후기 0개 | "첫 후기를 남겨보세요" (폼은 그대로 노출) |
| 후기 삭제 확인 모달 | "이 후기를 지울까요? 작성할 때 쓴 비밀번호를 입력해 주세요" / 비번 불일치(403): "비밀번호가 맞지 않아요" |
| 서버/네트워크 오류 | "잠시 연결이 원활하지 않아요" + [다시 시도] (기술 용어·상태코드 노출 금지) |
| 별점 라벨 | "별점 (선택)" |
| 비밀번호 헬퍼 | "후기를 지울 때만 써요 (4자 이상)" |

**DoD**: 각 상태를 실제로 재현(네트워크 차단·빈 필터 등)해 문구·UI 확인.

---

## R8 — 검수 체크리스트

- [x] 320·390·768·1024·1440 viewport 회귀 자동화 — document-level overflow와 터치 타깃 검사
- [x] "지금 제철" 뱃지: 이번 달 제철 데이터와 월 경계 fixture 검증
- [x] 별점 없는 후기와 가격/차트 empty·부분 실패 상태 분리
- [x] 이미지 alt·fallback, 키보드 포커스, dialog focus trap/restore, route focus/announce 자동화
- [x] light/dark Chromium·WebKit resilience matrix 자동화
- [x] 일반 텍스트 대비 기준을 token 단위 테스트로 검증
- [x] 브랜드 표기 FishNote, 측정 근거 없는 “인기/에디터 추천” 카피 제거
- [ ] 실제 기기 200% zoom·저속 폰트·원격 이미지 crop/URL·Vercel preview 시각 QA

### Phase 5 품질 보강에서 바뀐 구현 계약

- 카드 전체에 Link를 씌우고 내부에 버튼을 넣는 중첩 interactive 구조를 제거했다. 이미지/제목 링크와
  44px 저장 버튼은 형제 요소이며 각각 이동·저장 analytics를 한 번만 발생시킨다.
- `SmartImage`가 dimensions, lazy/priority, focal point, broken URL fallback을 공통 처리한다.
- 모바일 검색은 draft를 가진 filter sheet로 바뀌었고, 적용 전에는 URL·결과를 변경하지 않는다.
- 상세 첫 viewport는 이름·제철·가격 요약을 이미지보다 먼저 읽도록 DOM/모바일 순서를 조정했다.
- 가격은 반응형 SVG와 접근 가능한 table을 함께 제공하고 price/review/source를 독립 query 경계로
  나눠 한 섹션 오류가 전체 상세을 가리지 않게 했다.
- 홈은 세 번의 Fish 요청 대신 `GET /api/v1/home?month=N&sort=popular` 한 번으로
  seasonal/featured/catalog/facets를 받는다.
- 측정 전 카피는 “바로 찾아보기”, “후기 많은 순”, “처음 먹기 좋은 회”로 통일했다.
- 공개 상세은 slug 경로로 build-time prerender하며 private route는 noindex shell을 사용한다.

---

## 부록 A — 생선 사진(공개 자료) 운영 가이드

- **source of truth**: `config/fish_image_manifest.json`. `data.sql`이나 화면 컴포넌트에 대표 URL을
  수동 추가하지 않는다.
- **READY 조건**: catalog ID/name/slug 일치, 학명 또는 분류 범위 확인, 상업 이용 가능한 license,
  원문 `sourceUrl`, credit, 원본 width/height, 구체적 한국어 alt, focal point를 모두 기록한다.
- **현재 coverage**: V13 reviewed seed 기준 공개 26종 중 PRIMARY 26종(26/26), PHOTO 출처 26종.
  가자미는 특정 단일 종으로 오인하지 않게 `Pleuronectidae spp.`와 시장의 가자미류 사진/alt를 쓴다.
- **검사**: `validate_fish_image_manifest.py`, manifest unittest,
  `render_fish_image_seed.py --check`를 함께 실행한다. V13 적용 뒤에는 V13을 재생성하지 않고 새
  forward migration을 만든다.
- **표시**: FE는 `media` metadata를 우선하고 legacy `imageUrl/images`로 fallback한다. attribution은
  상세 화면의 출처 링크에서 제공한다.
- **운영 QA**: manifest 26/26은 실제 배포 origin의 HTTP 200·CORS·crop·외부 원본 영속성을 보장하지
  않는다. 이 항목은 Vercel preview/실기기에서 별도 확인한다.

## 부록 B — 사진 저장 방식 비교 (참고)

| 방식 | 사용자 경험 | 구현 난이도 | 비고 |
|---|---|---|---|
| URL 직접 입력 (기존) | 나쁨 — 일반 사용자 불가 | 0 | v1 화면에서 제거 |
| **BE 경유 Cloudinary (U1·U2, 채택)** | 파일 선택만 하면 됨 | 낮음 | 무료 티어 충분, 키는 BE에만, 리사이즈 자동 |
| FE에서 Cloudinary 직접 업로드(unsigned preset) | 동일 | 최저 | BE 무변경. 단 프리셋 남용 가능성 — 채택안이 막히면 대안 |
| S3/R2 presigned URL | 동일 | 중간 | 인프라 학습용으로 v2 후보 |
