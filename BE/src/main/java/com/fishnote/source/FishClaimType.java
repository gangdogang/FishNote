package com.fishnote.source;

/** API order is the declaration order; append new values only after an API version change. */
public enum FishClaimType {
    IDENTITY,
    SEASON,
    TASTE,
    PRICE,
    PHOTO
}
